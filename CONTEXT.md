# 哈希校验上下文

ezcheck 将字节输入转换为摘要，并把摘要与外部期望值进行校验；本上下文用于统一计算、比较和批量校验之间的语言与边界。

## Language

**输入源（Input Source）**：
一次计算所读取的字节来源，只能是文件、标准输入或直接文本之一。
_避免使用_：输入文件（无法涵盖标准输入和文本）

**哈希算法（Hash Algorithm）**：
把输入字节映射为固定格式摘要的命名算法。
_避免使用_：后端、哈希类型

**期望摘要（Expected Hash）**：
用于校验的外部摘要值，可以携带算法前缀。
_避免使用_：正确摘要、目标文件

**已解析摘要（Resolved Hash Input）**：
由期望摘要、一个或多个候选哈希算法及算法是否由摘要推断而成的不可变组合。
_避免使用_：检测结果、哈希任务

**校验记录（Verification Record）**：
一个输入源、一个期望摘要和该摘要候选算法的独立校验单元。
_避免使用_：文件组、算法组

**校验结果（Verification Outcome）**：
某个校验记录在单个候选算法下的匹配状态及实际摘要。
_避免使用_：消息、输出行

**校验文件（Check File）**：
由零个或多个 GNU shasum 兼容校验记录组成的文件。
_避免使用_：摘要文件、任务文件

**算法后端（Algorithm Backend）**：
编译时启用并为一组哈希算法提供实现的能力集合。
_避免使用_：运行模式、互斥配置

## Relationships

- 一个**输入源**可以被一个或多个**哈希算法**在同一次读取中计算
- 一个**已解析摘要**包含恰好一个**期望摘要**和一个或多个候选**哈希算法**
- 一个**校验记录**包含恰好一个**输入源**和一个**已解析摘要**
- 一个**校验记录**为每个候选**哈希算法**产生一个**校验结果**，匹配后可停止后续候选计算结果的展示
- 一个**校验文件**包含零个或多个彼此保持身份独立的**校验记录**
- 多个**算法后端**可以叠加启用；重复提供的算法由确定的优先级选择实现

## Example dialogue

> **开发者：** “两个校验行引用同一个输入源且摘要长度相同，可以合并成一个算法组吗？”
> **领域专家：** “不可以。每一行都是独立的校验记录；每条记录先解析自己的期望摘要和候选算法，再对输入源执行一次读取。”

## Flagged ambiguities

- “check” 曾同时表示 compare 子命令、批量校验和期望摘要；统一使用**校验记录**、**校验文件**和**期望摘要**区分这些概念
- “算法检测” 曾混合显式参数、摘要前缀和长度推断；统一由**已解析摘要**记录解析后的候选集与推断来源
- `-` 在输入位置表示标准输入，不表示名为 `-` 的文件路径
- 相同路径或相同摘要长度不代表同一**校验记录**，批量解析不得据此合并记录
