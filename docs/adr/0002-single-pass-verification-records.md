# 每条校验记录只读取输入一次

同一摘要长度可能对应多个候选算法，而标准输入无法可靠地重复读取。校验因此以独立记录为边界：一次读取同时更新该记录的所有候选哈希器，批量校验也不得按路径或摘要长度合并记录，从而兼顾流式输入的正确性和记录身份语义。

## Consequences

- 候选算法数量只增加哈希计算，不增加文件或标准输入读取次数
- 每个校验文件行独立成功或失败，即使多行引用同一路径
- 多条记录引用标准输入时仍是多次独立读取，调用方应避免这种含义不明确的校验文件
